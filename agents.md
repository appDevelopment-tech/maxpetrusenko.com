# Agent Guide

- Work in `nextjs/` for live site changes; root HTML is legacy.
- Keep `GEO_SEO_TODO.md` aligned with current implementation.
- Do not edit subdomain files in `atelier/` or `mindfold/` unless requested.
- Run `npm run lint` before deploy.
- Deploy via `npm run deploy` and verify `https://www.maxpetrusenko.com/`.
